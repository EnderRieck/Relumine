# @package _global_
defaults:
  - override /model: paddleocr_vl

task: evaluate
experiment:
  name: eval_paddle
data:
  split: test
eval:
  enabled: true
  mode: page
  prompt_name: free_ocr
train:
  enabled: false
